order = '99'
module = 'core-feature-server'

instdir = modulesdir / '@0@@1@'.format(order, module)

install_data(
        'module-setup.sh',
        'ubuntu-core-server.conf',
        install_dir : instdir,
)
