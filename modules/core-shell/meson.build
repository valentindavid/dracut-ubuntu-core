order = '99'
module = 'core-shell'

instdir = modulesdir / '@0@@1@'.format(order, module)

install_data(
        'module-setup.sh',
        'emergency.conf',
        'emergency-target.conf',
        'debug-shell.conf',
        install_dir : instdir,
)
